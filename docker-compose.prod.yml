services:
  api:
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000

  db:
    restart: always
  redis:
    restart: always
